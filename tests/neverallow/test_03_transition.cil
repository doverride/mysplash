; type of process to transition to is not associated with subject

(block test
    (type source)
    (call subject (source))

    (type target)
    (call object_roletype (target))

    (allow test.source test.target (process (transition)))
)
